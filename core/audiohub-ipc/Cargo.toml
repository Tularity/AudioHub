[package]
name = "audiohub-ipc"
version = "1.0.1"
edition = "2021"

[dependencies]
audiohub-core = { path = "../audiohub-core" }
audiohub-net = { path = "../audiohub-net" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
